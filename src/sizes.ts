// All sizes in pixels

export const radius = 10;

export const columnGap = radius*1.5;

export const rowGap = {
    standard: radius * 1.5,
    fifthRow: radius * 2,
};

export const boardPadding = 0;